# disaster_vision

## Google Cloud

Use `cloud/README.md` to run the existing model training script and the
OpenEvolve loop on a Google Cloud GPU VM.

The same runbook includes a process artifact capture step that creates ordered
PNG frames, a storyboard, and optional MP4 evidence for explaining the
OpenEvolve workflow.
